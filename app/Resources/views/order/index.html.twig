{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            <ul>
                {% for order in orders %}
                    <li>#{{ order.id }} - {{ order.description }} <a href="{{ path('refund', {id: order.id}) }}">Refund</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}
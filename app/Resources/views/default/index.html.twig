{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            <ul>
                {% for product in products %}
                    <li>{{ product.title }} - <a href="{{ path('buy', {id: product.id}) }}">Buy</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}